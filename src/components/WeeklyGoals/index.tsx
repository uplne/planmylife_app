import { Box } from '../Box';
import { SubHeading } from '../SubHeading';
import { TasksBox } from '../TasksBox';

export const WeeklyGoals = () => {
  return (
    <TasksBox>
      <Box>
        <SubHeading title="Goals and Projects" />
      </Box>
    </TasksBox>
  );
};